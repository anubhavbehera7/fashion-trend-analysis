apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

commonLabels:
  environment: production

# Production: use base replica counts + HPA
# No replica patches needed — base values are production-ready
